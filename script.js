// prompt the user for two numbers and an operator

let x = (prompt ("50"));
let y = (prompt ("100"));
let operator = prompt ("+, -, *, /");

let result;

// perform the calculations
if ("x || y") {
    result = "invalid input, enter a numeric number"
} else {
    switch (operator) {
        case "+":
            result = x + y 
            console.log (" x + y");
    

}